/**
 * Pugs are slightly less cute when running on Android
 * @flow
 */

import { MainApplication } from "./src"
import { AppRegistry } from "react-native"

AppRegistry.registerComponent("Reactipug", () => MainApplication)
